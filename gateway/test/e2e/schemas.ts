import { IUser, UserSchema } from '../../../user/src/schemas/user.schema';
import {
  IUserLink,
  UserLinkSchema,
} from '../../../user/src/schemas/user-link.schema';
import { IToken, TokenSchema } from '../../../token/src/schemas/token.schema';
import { ITask, TaskSchema } from '../../../task/src/schemas/task.schema';

export {
  UserSchema,
  UserLinkSchema,
  TokenSchema,
  TaskSchema,
  IUser,
  IUserLink,
  IToken,
  ITask,
};
